export interface Fonction {
    fonctionId: number;
    nonFonction: string;
    createDate: string; // ou Date, si tu préfères utiliser le type Date natif
    updateDate: string; // ou Date, si tu préfères utiliser le type Date natif
}
